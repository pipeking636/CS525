作业描述：
我有个复杂的C语言编程作业，请看上传的目录中的Instructions.md文件，它对作业要求做了明确细致的说明，请先阅读。阅读后按下述步骤实施
一. 浏览文件夹中所有的源代码，了解源代码的功能和结构。
二. 仔细阅读bptree.md文件，它对B+树的操作逻辑做了比较详细的说明和定义，而文件夹中的bptree.c是根据这个定义实现的，也需要仔细配合阅读。
三. 阅读代码中已经存在的btree_mgr.c，它只是一个框架，为了防止编译错误。但是其中定义好了函数接口，这些接口会被test_assign4_1.c测试程序调用，非常重要。
四. 阅读test_assign4_1.c，了解测试流程。它会调用btree_mgr.c中被定义的函数。
五. 作业的目标是实现一个完善的btree_mgr.c文件，并让其通过test_assign4_1.c的测试。
六. 参考bptree.c，它的逻辑比较完善。把bptree.c中的代码merge到btreee_mgr.c中。请尽量复用bptree.c文件的中的代码，尽可能不修改它的逻辑，否则很可能出现不必要的麻烦。
七. 参考Instructions.md中对log输出的输出形式结合原来bptree.c的log文件的输出方式，在bptree_mgr.c中创建一个可靠的log文件输出函数，将详细的log信息输出到文件，同时在终端输出关键信息。在终端输出关键信息的时候请用下面的宏定义来实现，方便在makefile里进行开关处理。
``` C
#ifdef DEBUG // define this macro from makefile to enable debug print
    #define DEBUG_PRINT(format, ...) printf(format, ##__VA_ARGS__)
#else
    #define DEBUG_PRINT(format, ...)
#endif
```
*八. 实现过程中的注意点：*
1. 特别留意节点的分裂操作，它必须兼容不同的最大键值数量，比如奇数和偶数的分裂方式不同（参考bptree.c和bptree.md）! 可以考虑用一个全局变量来获取节点中最大Key数量的定义，取代原来的"#define MAX_KEYS 2 "宏定义。
2. 请特别小心，要先插入键后再分裂，否则容易出现很多问题（参考bptree.c和bptree.md）!
3. 在实现时，函数的状态返回值请从dberror.h文件中查找，如果没有合适的返回值定义，可以添加到dberror.h中，不要随意的使用返回值。最后，为了调试方便，请在实现的时候就在代码中加入一些关键节点的log输出信息。
4. 在实现时请密切关注内存操作的部分，尽可能避免内存访问错误！
5. 第一次生成代码时请给出所有更改代码的完整实现，不要给我修改片段。以后在修改时可以给出修改片段，但是确保是函数完整的，不要给我零星的代码片段。

综上，以上请仔细理解并帮我实现完整的可以编译通过并通过test_assign4_1.c的代码。